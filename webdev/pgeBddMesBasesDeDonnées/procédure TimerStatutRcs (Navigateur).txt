// Procédure automatique :
// La procédure est exécutée automatiquement, après le code d'initialisation de la page
// Elle sera répétée en boucle, en attendant 1 seconde entre chaque appel
// Chaque appel suivant exécute une seule fois la procédure, sans timer
// 

procédure TimerStatutRcs()

// Toutes les 5 s (automatisme de la procédure) : appel serveur seulement si une base est en attente ou en cours
si _bRcsEnCours = 1 alors
	RafraichitStatutRcs()
fin