// Résumé : <indiquez ici ce que fait la procédure>
// Syntaxe :
//[ <Résultat> = ] BloctelStatutEnCoursDeTraitement (<pIdBloctel>, <pFichier>)
//
// Paramètres :
//	pIdBloctel : <indiquez ici le rôle de pIdBloctel>
//	pFichier : <indiquez ici le rôle de pFichier>
// Valeur de retour :
// 	multi-valeur : <indiquez ici le rôle de la valeur de retour>
//
// Exemple :
// <Indiquez ici un exemple d'utilisation>
//
procédure BloctelStatutEnCoursDeTraitement(local pIdBloctel,local pFichier)

nNbrFichier		est un entier 
sStatut			est une chaîne
sFichierSortie	est une chaîne 

nNbrFichier = FTPListeFichier(__nIdSftp,"/OUT/*" + __ZIP__, FTPListeFichier_Callback)

renvoyer (sStatut, sFichierSortie)

	procédure interne FTPListeFichier_Callback(sFichier chaîne, nTaille entier <utile>, sAttribut chaîne <utile>,  sDate chaîne <utile>, sHeure chaîne <utile>, vParamètreDeLaProcédure Variant <utile>) : booléen

		si Contient(sFichier,pFichier) alors
			si ope_bloctel.LitRecherchePremier(IdBloctel,pIdBloctel) alors
				ope_bloctel.FichierSortie				= fExtraitChemin(sFichier,fFichier)
				ope_bloctel.DateHeureFinTraitement		= DateHeureSys()
				ope_bloctel.Statut					= __STATUT_BLOCTEL_TRAITE__
				si ope_bloctel.Modifie() alors
					sStatut 			= __STATUT_BLOCTEL_TRAITE__
					sFichierSortie	= fExtraitChemin(sFichier,fFichier)
				fin
			fin
		fin
		renvoyer Vrai	
		
	fin