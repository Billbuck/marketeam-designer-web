// Résumé : <indiquez ici ce que fait la procédure>
// Syntaxe :
//[ <Résultat> = ] BloctelStatutEnAttente (<pIdOperation>, <pIdBloctel>)
//
// Paramètres :
// 	pIdOperation : <indiquez ici le rôle de pIdOperation>
//	pIdBloctel : <indiquez ici le rôle de pIdBloctel>
// Valeur de retour :
// 	Type indéterminé : <indiquez ici le rôle de la valeur de retour>
//
// Exemple :
// <Indiquez ici un exemple d'utilisation>
//
//	pIdBaseSource : <indiquez ici le rôle de pNIdBaseSource>
procédure BloctelStatutEnAttente(pIdOperation, pIdBloctel)


si HLitRecherchePremier(ope_bloctel,IdBloctel,pIdBloctel) alors
	ope_bloctel.Statut		= __STATUT_BLOCTEL_PRISE_EN_CHARGE__
	si ope_bloctel.Modifie() alors
		si dos_operation.LitRecherchePremier(IdOperation,pIdOperation) alors
			dos_operation.Statut				 	= __Statut_Prise_En_Charge__
			dos_operation.DateHeurePriseEnCharge	= DateHeureSys()
			si dos_operation.Modifie() alors
				renvoyer (__STATUT_BLOCTEL_PRISE_EN_CHARGE__)
			fin
		fin
	fin
fin

renvoyer ("")

