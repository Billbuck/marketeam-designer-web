// Résumé : Lance une procédure de paiement sécurisé pour un paiement via un site de banque
// Syntaxe :
//[ <Résultat> = ] Paiement (<pclPaiementSécurisé> est PaiementSécurisé dynamique, <moMontant> est monétaire, <sAdresseEmail> est chaîne, <stPaiementParametre> est structPaiementParametre)
// Paramètres :
//	pclPaiementSécurisé (PaiementSécurisé dynamique) : Configuration du paiement sécurisé
//	moMontant (monétaire) : Montant de la transaction
//	sAdresseEmail (chaîne ANSI) : Adresse email du client
//	stPaiementParametre (structPaiementParametre) : <indiquez ici le rôle de stPaiementParametre>

// Valeur de retour :
// 	booléen : Vrai si la procédure de paiement a bien été exécutée, Faux sinon
//
procédure Paiement(pclPaiementSécurisé est un PaiementSécurisé dynamique, local moMontant est un monétaire, local sAdresseEmail est une chaîne, local stPaiementParametre est une structPaiementParametre) <zombie = "il faut utiliser 'Paiement_DepuisBanque' ou 'Paiement_Direct' "> : booléen 

renvoyer Paiement_DepuisBanque(pclPaiementSécurisé, moMontant, sAdresseEmail, stPaiementParametre)

