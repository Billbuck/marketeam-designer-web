procédure mixCarteOk()

dDate est une Date

si _sCarteBancaireMois <> "" _et_ _sCarteBancaireAnnée <> "" alors
	dDate.Année	= "20" + _sCarteBancaireAnnée
	dDate.Mois	= _sCarteBancaireMois
	dDate.Jour	= 1
	dDate		= DernierJourDuMois(dDate)
fin

si _bCarteBancaireMémorisée = Faux alors
	si CaractOccurrenceType(saiCarteNumero,ctNumérique) pas dans (15,16)	alors	renvoyer Faux
	si _sCarteBancaireMois = ""												alors 	renvoyer Faux
	si _sCarteBancaireAnnée = ""											alors 	renvoyer Faux
	si dDate < DateSys()													alors 	renvoyer Faux
	si Taille(saiCarteCode) < 3 											alors	renvoyer Faux
	si mixVerificationCarteBancaire(saiCarteNumero) = Faux 					alors	renvoyer Faux
fin

renvoyer Vrai