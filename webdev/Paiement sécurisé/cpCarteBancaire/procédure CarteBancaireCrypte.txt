procédure CarteBancaireCrypte(pValeur)


sMessage	est un Buffer	= pValeur
// ⚠ MIROIR : phrase secrète MASQUÉE — reprendre EXACTEMENT la même phrase
// que dans mdlcPaiement.CarteBancaireCrypte (jamais de secrets dans le dépôt Git).
bufCle		est un Buffer	= HashChaîne(HA_MD5_128, "***MASQUE-VOIR-PROJET-WEBDEV***")
bufCrypte	est un Buffer	= CrypteStandard(sMessage, bufCle, crypteAES128)

renvoyer bufCrypte
