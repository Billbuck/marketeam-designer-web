// Résumé : Journalise une erreur de traitement du webhook capability check RCS.
// Une seule écriture fichier par erreur (le fichier n'est ouvert qu'une fois).
procédure EnregistreErreurRcsCapacite(pTitre, pRequete)

sLigne est une chaîne

sLigne = RC + DateHeureVersChaîne(DateHeureSys(), "JJ/MM/AAAA HH:mm:SS")
sLigne += RC + pTitre
sLigne += RC + pRequete
sLigne += RC + ErreurInfo(errComplet)
sLigne += RC + "------------------------------------------------------------------------------------------------"

fAjouteTexte(fRepDonnées() + "\RcsErreurCapacite.txt", sLigne)
