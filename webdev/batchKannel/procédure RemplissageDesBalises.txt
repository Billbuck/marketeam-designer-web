// Résumé : <indiquez ici ce que fait la procédure>
// Syntaxe :
//[ <Résultat> = ] RemplissageDesBalises (<sMessage>, <bEstCliquezIci>, <taaBaseChamp> est tableau associatif (structBaseChamp))
//
// Paramètres :
//	sMessage : <indiquez ici le rôle de sMessage>
//	bEstCliquezIci : <indiquez ici le rôle de bEstCliquezIci>
//	taaBaseChamp (tableau associatif (structBaseChamp)) : <indiquez ici le rôle de taABaseChamp>

// Valeur de retour :
// 	chaîne ANSI : <indiquez ici le rôle de la valeur de retour>
//
// Exemple :
// Indiquez ici un exemple d'utilisation.
//
//	tabChamps : <indiquez ici le rôle de tabChamps>
procédure RemplissageDesBalises(local sMessage, local bEstCliquezIci, local taaBaseChamp est un tableau associatif de structBaseChamp)

sListeChamps	est une chaîne
sSelectRetour	est une chaîne

pour tout stBaseChamp, sLibelle de taaBaseChamp
	si Contient(sMessage,"[" + sLibelle + "]") alors
		sListeChamps += [","] + sLibelle
	fin
fin

sSelectRetour = "'" + Remplace(sMessage,"'","''") + "'"

pour toute chaînes sLibelle de sListeChamps séparée par ","
	sSelectRetour = "REPLACE(" + sSelectRetour + ",'[" + sLibelle + "]', IFNULL(" + taaBaseChamp[sLibelle].Champ + ",''))"
fin

si bEstCliquezIci alors
	si Contient(sMessage,"[" + __CHAMP_CLIQUEZ_ICI__ + "]") alors
		sSelectRetour = "REPLACE(" + sSelectRetour + ",'[" + __CHAMP_CLIQUEZ_ICI__ + "]', IFNULL(" + __CHAMP_CLIQUEZ_ICI__ + ",'XXXXXX'))"
	fin
fin

renvoyer sSelectRetour
