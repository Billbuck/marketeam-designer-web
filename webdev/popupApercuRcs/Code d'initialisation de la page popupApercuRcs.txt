// ** Déclarations globales de popupApercuRcs (Serveur)
//
// Popup d'aperçu RCS : un sélecteur Android / iPhone (deux boutons),
// une case « Afficher le repli SMS » (visible seulement si EstRepliSms)
// et un champ HTML ztrEcran de 380 x 780 px. Pas d'image de fond : le
// cadre du téléphone est dessiné en CSS par le moteur d'aperçu
// (ConstruitHtmlApercu), avec ses variantes iPhone / Android.
//
// Aucune page ni adresse : ChargeEcran pose dans ztrEcran..LibelléHTML
// un iframe alimenté par l'attribut srcdoc (le JavaScript s'y exécute
// normalement). Le JSON d'aperçu est calculé UNE seule fois ici et gardé
// dans la globale de page __sJsonApercu ; le HTML de l'écran est
// reconstruit à chaque changement de mode (procédure locale
// ConstruitHtmlApercu).

procédure MaPage(_stOperationRcs est une structOperationRcs = Null, _pIdOperation = 0)

__sModeEcran	est une chaîne = "android"	// dernier mode Android / iPhone choisi
__sJsonApercu	est une chaîne						// JSON d'aperçu, calculé une seule fois


// ** Fin d'initialisation de popupApercuRcs (Serveur)

__sJsonApercu = ConstruitJsonApercuRcs(_stOperationRcs, _pIdOperation)

// La case « Afficher le repli SMS » n'est visible que si le repli existe
ckRepli..Visible	= _stOperationRcs.EstRepliSms
ckRepli				= Faux

ChargeEcran("android")
